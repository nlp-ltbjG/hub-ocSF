准确率方面：
LLM SFT 表现最佳（58%），略高于传统的 BERT Fine-Tuning（55.26%）。这说明在特定任务上，经过指令微调的小参数量LLM也能达到甚至超越传统BERT模型的效果。LLM Zero-Shot 效果最差（36%），表明在没有特定领域数据微调的情况下，通用大模型直接处理特定分类任务存在局限。
训练资源与效率：
BERT Fine-Tuning 训练速度较快，但显存占用相对较高（全参数微调）。
LLM SFT 虽然训练时间最长，但由于使用了LoRA技术，显存占用反而较低（3GB），更适合在资源受限的环境下进行大模型适配。
LLM Zero-Shot 无需训练，推理速度最快，适合快速验证。
输出稳定性：
BERT 输出结构化概率，最为稳定。
LLM Zero-Shot 输出格式不可控，容易出现无法解析的文本（如解释性文字而非类别名），导致约29%的样本失效。
LLM SFT 通过指令微调，极大地规范了模型的输出格式，可解析率提升至99%。
性能排序：LLM SFT > BERT Fine-Tuning > LLM Zero-Shot
